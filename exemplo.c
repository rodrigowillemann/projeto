

main(){
    //Limite de crédito:
    float limite[100];
    //.....

    printf("Informe o valor do limite: ")
    scanf("%f", limite[i]);
    if(limite[i] >= 100 && limite[i] <= 500)
        //validação

}
